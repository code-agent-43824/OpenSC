# Статус

Ветка `master` синхронизирована с `origin/master`. Штатные CI OpenSC собирают проект, но отдельного portable-релиза `pkcs11-tool` и `pkcs11-spy` с независимой проверкой архивов пока нет.

В работе: матрица из шести платформ по образцу portable-релиза `code-agent-43824/SoftHSMv2`.

