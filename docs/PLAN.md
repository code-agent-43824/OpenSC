# План

## Portable-сборки pkcs11-tool и pkcs11-spy

- [~] Добавить воспроизводимые portable-сборки для Linux x64/ARM64, Windows x86/x64/ARM64 и macOS universal.
- [ ] Упаковывать `pkcs11-tool` и `pkcs11-spy` без сторонних динамических зависимостей.
- [ ] После каждой сборки проверять архив на отдельном чистом GitHub Actions runner той же платформы.
- [ ] На проверочном runner скачивать обычный архив последнего релиза `code-agent-43824/SoftHSMv2` для соответствующей платформы.
- [ ] Инициализировать токен через собранный `pkcs11-tool`, создать, записать, прочитать и удалить data object.
- [ ] Повторить сценарий через собранный `pkcs11-spy` и проверить полноту и порядок ключевых вызовов в его логе.
- [ ] Публиковать только архивы, прошедшие все шесть независимых проверок, и общий `SHA256SUMS`.

