# План

## Portable-сборки pkcs11-tool и pkcs11-spy

- [x] Добавить воспроизводимые portable-сборки для Linux x64/ARM64, Windows x86/x64/ARM64 и macOS universal.
- [x] Упаковывать `pkcs11-tool` и `pkcs11-spy` без сторонних динамических зависимостей.
- [x] После каждой сборки проверять архив на отдельном чистом GitHub Actions runner той же платформы.
- [x] На проверочном runner скачивать обычный архив последнего релиза `code-agent-43824/SoftHSMv2` для соответствующей платформы.
- [x] Инициализировать токен через собранный `pkcs11-tool`, создать, записать, прочитать и удалить data object.
- [x] Повторить сценарий через собранный `pkcs11-spy` и проверить полноту и порядок ключевых вызовов в его логе.
- [x] Публиковать только архивы, прошедшие все шесть независимых проверок, и общий `SHA256SUMS`.
- [x] На каждом проверочном runner собирать и запускать автономный `opensc-testkit-<platform>` с обоими инструментами, обычным portable-модулем SoftHSM и точным тестовым сценарием.
- [x] Публиковать шесть проверенных test kit как плоские GitHub Actions artifacts и как отдельные release assets без вложенных ZIP.
- [x] Отключить автоматические workflow, которые напрямую не собирают и не тестируют `pkcs11-tool` или `pkcs11-spy`.

## Расширенные функции PKCS #11 Рутокен

Полный перечень, ABI, требования к CLI, spy и тестам зафиксированы в
[`RUTOKEN-EXTENSIONS.md`](RUTOKEN-EXTENSIONS.md). Основа — официальные
заголовки Aktiv для `rtPKCS11ECP` 2.19.0.0.

### Этап 1 — ABI и обнаружение

- [~] Независимо описать типы, константы, 33 сигнатуры и
  `CK_FUNCTION_LIST_EXTENDED`, не копируя заголовки вендора.
- [x] Добавить compile-time ABI-проверки для Windows x86/x64/ARM64 и Unix
  x64/ARM64, а также модуль-заглушку с эталонной таблицей.
- [x] Научить общий загрузчик опционально находить
  `C_EX_GetFunctionListExtended`, не меняя поведение обычных модулей.

### Этап 2 — полная прозрачность `pkcs11-spy`

- [x] Экспортировать расширенную точку входа и вернуть таблицу из 33 оберток в
  точном порядке ABI 2.19.0.0.
- [x] Передавать и безопасно логировать все актуальные и семь устаревших
  функций; секретные значения по умолчанию редактировать.
- [x] Проверить каждый указатель таблицы модулем-заглушкой, включая ошибки,
  двухпроходные буферы и вызовы до `C_Initialize`.

### Этап 3 — безопасный read-only CLI `pkcs11-tool`

- [x] Добавить обнаружение расширения и первые команды для
  `GetTokenInfoExtended` и `GetTokenName`.
- [ ] Добавить команды для `GetLicense`, `GetJournal`, `GetVolumesInfo`,
  `GetDriveSize` и `GetCertificateInfoText`.
- [ ] Добавить текстовый и JSON-вывод с фиксированными тестами сериализации.

### Этап 4 — криптография и аутентификация

- [ ] Добавить команды `PKCS7Sign`, все четыре `PKCS7Verify*`, `CreateCSR` и
  гарантированный `FreeBuffer` на успешных и ошибочных путях.
- [ ] Добавить `Authenticate`, `Deauthenticate`, `UnblockAuthenticator` и
  безопасный ввод аутентификационных данных.

### Этап 5 — изменяющие и разрушительные операции

- [ ] Добавить `InitToken`, `UnblockUserPIN`, `SetTokenName`, `SetLicense`,
  `SetLocalPIN`, `TokenManage`, `SlotManage`, `ChangeVolumeAttributes` и
  `FormatDrive` с обязательным явным подтверждением разрушительных режимов.
- [ ] Добавить явно помеченные legacy-команды для семи устаревших функций,
  сохранив полную совместимость spy и таблицы.

### Этап 6 — межплатформенная и аппаратная приемка

- [x] Включить ABI/stub-тесты расширения во все шесть portable build/verify
  пар и в публикуемые test kit.
- [ ] Проверить доступный профиль на portable SoftHSM fork, затем выполнить
  отдельную аппаратную приемку flash, лицензий, журнала и аутентификаторов на
  совместимом Рутокене; hardware skip должен быть видимым.
