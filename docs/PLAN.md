# План

## Portable-сборки pkcs11-tool и pkcs11-spy

- [x] Добавить воспроизводимые portable-сборки для Linux x64/ARM64, Windows x86/x64/ARM64 и macOS universal.
- [x] Упаковывать `pkcs11-tool` и `pkcs11-spy` без сторонних динамических зависимостей.
- [x] После каждой сборки проверять архив на отдельном чистом GitHub Actions runner той же платформы.
- [x] На проверочном runner скачивать обычный архив последнего релиза `code-agent-43824/SoftHSMv2` для соответствующей платформы.
- [x] Инициализировать токен через собранный `pkcs11-tool`, создать, записать, прочитать и удалить data object.
- [x] Повторить сценарий через собранный `pkcs11-spy` и проверить полноту и порядок ключевых вызовов в его логе.
- [x] Публиковать только архивы, прошедшие все шесть независимых проверок, и общий `SHA256SUMS`.
