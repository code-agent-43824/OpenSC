# Передача работы

Незавершенной работы сейчас нет.

Локальный `pkcs11-spy.conf`, fallback, шаблоны и тесты завершены. Release run
`32370121050`, релиз `0.27.1-portable.4`; все assets и Linux x64 test kit
повторно проверены после публикации.

Следующая отдельная работа из плана — остальные read-only команды Рутокен и
JSON-вывод.
